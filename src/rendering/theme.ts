/** Scene colours (light theme). Kept in sync with the `lab-*` tokens in `src/index.css`. */
export const sceneColors = {
  /** Very light blue-white, so white UI panels still read as separate from the scene. */
  background: '#f4f8fd',
  gridCell: '#dbe6f3',
  gridSection: '#a9bfdb',
  axisX: '#e5484d',
  axisY: '#2f9e44',
  axisZ: '#1d6fe0',
  axisLabel: '#ffffff',
} as const
